{
  "name": "@nexirift/db",
  "version": "0.0.1",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "db:start-dev": "./dev/start-database.sh",
    "db:all": "bun db:generate && bun db:migrate && bun db:studio"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@types/bun": "latest",
    "@types/pg": "^8.11.11",
    "drizzle-kit": "^0.30.4"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.3",
    "pg": "^8.13.3"
  }
}
